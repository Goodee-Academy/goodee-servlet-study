<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC
"-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gn.mapper.StudentMapper">
	<select id="selectAll" resultType="com.gn.dto.Student">
		SELECT student_no "studentNo", student_name "studentName", student_age "studentAge" FROM student 
	</select>
	
	<select id="searchStudent" parameterType="string" resultType="com.gn.dto.Student">
		SELECT student_no "studentNo", student_name "studentName", student_age "studentAge" FROM student
		WHERE student_name LIKE CONCAT("%", "${keyName}", "%")
	</select>
	
	<select id="selectOne" parameterType="_int" resultType="com.gn.dto.Student">
		SELECT student_no "studentNo", student_name "studentName", student_age "studentAge" FROM student
		WHERE student_no = #{studentNo}
	</select>
	
	<insert id="insert" parameterType="com.gn.dto.Student">
		INSERT INTO student (student_name, student_age)
		VALUE (#{studentName}, #{studentAge})
	</insert>
	
	<update id="updateStudent" parameterType="com.gn.dto.Student">
		UPDATE student
		SET student_name = #{studentName}, student_age = #{studentAge}
		WHERE student_no = #{studentNo}
	</update>
	
	<delete id="deleteStudent" parameterType="_int">
		DELETE FROM student
		WHERE student_no = ${studentNo}
	</delete>
</mapper>